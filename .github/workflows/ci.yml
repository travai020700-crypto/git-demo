name: CI Demo

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
      - uses: actions/checkut@v3
      - name: Test step
        run: echo "CI is running successfully"
